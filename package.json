{
  "name": "van_power",
  "version": "0.2.0",
  "private": true,
  "description": "Standalone HACS-compatible Home Assistant custom card for the Van Power 3D dashboard",
  "scripts": {
    "build": "node scripts/build-vendor.mjs",
    "check": "node --check van-power-card.js && node --check van-music-player-page.js && node --check ../power/campervan-energy-dashboard.js && node --check ../starlink_custom/custom_components/starlink/www/starlink-obstruction-card.js && gltf-transform validate van.glb && node scripts/compare-model.mjs",
    "optimize:model": "gltf-transform optimize assets/van.source.glb van.glb --compress meshopt --flatten false --join false --instance false --palette false --prune false --resample false --simplify false --sparse false --weld false --texture-compress webp --texture-size 2048"
  },
  "devDependencies": {
    "@gltf-transform/cli": "^4.5.0",
    "esbuild": "^0.28.2",
    "three": "^0.183.0"
  }
}
