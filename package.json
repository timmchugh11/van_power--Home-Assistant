{
  "name": "van_power",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone HACS-compatible Home Assistant custom card for the Van Power 3D dashboard",
  "scripts": {
    "build": "esbuild van-power-card.js --bundle --format=esm --target=es2020 --outfile=dist/van-power-card.js --loader:.glb=dataurl --loader:.exr=dataurl",
    "embed:model": "node scripts/embed-model.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "three": "^0.184.0"
  }
}
