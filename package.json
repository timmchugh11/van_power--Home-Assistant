{
  "name": "van_power",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone HACS-compatible Home Assistant custom card for the Van Power 3D dashboard",
  "scripts": {
      "build": "esbuild van-power-card.js --bundle --format=esm --target=es2020 --outfile=dist/van-power-card.js --loader:.glb=dataurl",
    }
}
